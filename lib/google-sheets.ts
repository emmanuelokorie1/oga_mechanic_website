
export const appendToSheet = async (data: string[]) => {
  // Mock function for now unless user provides GOOGLE_SHEET_ID and service account credentials
  // Real implementation would require 'googleapis' package and complex auth setup
  console.log("Mock appending to sheet:", data);
  return Promise.resolve();
};
